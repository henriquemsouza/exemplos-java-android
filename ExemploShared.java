package com.hm.souza.shared;import android.content.SharedPreferences;import android.support.v7.app.AppCompatActivity;import android.os.Bundle;import android.view.View;import android.widget.Button;import android.widget.EditText;import android.widget.TextView;import android.widget.Toast;public class MainActivity extends AppCompatActivity {    private EditText txtNome;    private Button BotaoSalvar;    private TextView TXTexibição;    private static final String ARQUIVO_PREFERENCIA="";    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_main);        txtNome=(EditText) findViewById(R.id.editTextID);        TXTexibição=(TextView)findViewById(R.id.txtExibID);        BotaoSalvar=(Button)findViewById(R.id.btnSavID);        BotaoSalvar.setOnClickListener(new View.OnClickListener() {            @Override            public void onClick(View v)            {                SharedPreferences sharedPreferences=getSharedPreferences(ARQUIVO_PREFERENCIA,0);                SharedPreferences.Editor editor=sharedPreferences.edit();                if (txtNome.getText().toString().equals(""))                  {                      Toast.makeText(MainActivity.this,"por favor preencha  o campo  nome",Toast.LENGTH_SHORT).show();                  }else                      {                          editor.putString("nome",txtNome.getText().toString());                          editor.commit();                          TXTexibição.setText("OLa "+txtNome.getText().toString());                      }            }        });        //recuperar dados salvos        SharedPreferences sharedPreferences=getSharedPreferences(ARQUIVO_PREFERENCIA,0);        if (sharedPreferences.contains("nome"))        {              String nomeUsuario = sharedPreferences.getString("nome","usuario nao definido");              TXTexibição.setText(nomeUsuario);        }else            {                TXTexibição.setText("Ola usuario nao definido");            }    }}
